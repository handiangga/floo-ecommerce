"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { AdminService } from "@/services/admin.service";
export default function AdminLoginPage() { const router = useRouter(); const [error, setError] = useState(""); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const values = new FormData(event.currentTarget); try { const result = await AdminService.login(String(values.get("email")), String(values.get("password"))); localStorage.setItem("admin_access_token", result.data.token); router.push("/admin"); } catch { setError("Email atau password admin tidak valid."); } }; return <main className="grid min-h-screen place-items-center bg-muted p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl bg-white p-7 shadow-sm"><p className="text-primary text-sm font-medium">FLOO FASHION</p><h1 className="font-luxury mt-2 text-4xl">Admin Login</h1><input required name="email" type="email" placeholder="Email" className="mt-7 w-full rounded-xl border p-3" /><input required name="password" type="password" placeholder="Password" className="mt-3 w-full rounded-xl border p-3" />{error && <p className="mt-3 text-sm text-destructive">{error}</p>}<button className="mt-5 w-full rounded-full bg-primary py-3 text-white">Sign In</button></form></main>; }
